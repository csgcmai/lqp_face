import numpy as np

data=np.load('view2.npy')
for k in range(10):  # 10 fold
	tdata = []    
        for t in range(6):
           print k, t           
           tdata.append(data[t][0][k])
           print len(tdata)
           
        for t in tdata:
        	print t.shape
